{
  "name": "issue-to-project-action",
  "version": "1.0.0",
  "description": "GitHub Action: add newly opened issues to a GitHub ProjectV2",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit",
    "list-projects": "ts-node scripts/list-projects.ts",
    "list-project-statuses": "ts-node scripts/list-project-statuses.ts"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@octokit/graphql": "^8.1.1",
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}
